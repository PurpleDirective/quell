node_modules/
*.zip
*.pem
.DS_Store
dist/
# Chrome writes indexed dNR rulesets into unpacked extension dirs; never commit
# (leading-underscore dirs are also rejected by the Web Store packer)
_metadata/
tests/.profile/
